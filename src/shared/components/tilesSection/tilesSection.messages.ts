/* eslint-disable max-len */
import { defineMessages } from 'react-intl';

const scope = 'tilesSection';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Działalność',
  },
});
